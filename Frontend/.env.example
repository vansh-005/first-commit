# Base URL of the deployed API Gateway HTTP API, e.g.
# https://<api-id>.execute-api.ap-south-1.amazonaws.com
# Do not hardcode this value in source; each environment supplies its own.
VITE_API_BASE_URL=
